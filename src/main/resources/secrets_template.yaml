password: Admin UI password
secrets_key: A query argument that can replace the login form and the password

smtp:
    user: SMTP username
    password: SMTP password
    recipient: where motion emails should e send to

camera1:
    stream_uri: URL that can be passed to FFMPEG for snapshot extraction
